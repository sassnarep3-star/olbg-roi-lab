# Core pipeline runs on the Python standard library — no required installs.
# Install the optional ML stack for real feature engineering (M2+):
#   pip install -e ".[ml]"
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
requests>=2.31
